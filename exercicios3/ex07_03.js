// const prompt = require('prompt-sync')();

let = meses = ["janeiro", 'fevereiro', 'março', 'Abril', "maio", 'junho', 'julho', 'agosto', 'setembro', 'outubro', 'novembro', 'dezembro']
let = verificacao = "j"
for(mes of meses){
    if(mes.includes(verificacao)) {
        console.log(mes);
    }
}